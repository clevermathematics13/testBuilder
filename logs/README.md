# This directory holds GAS execution logs fetched from Google Drive.
# Log files (*.log) are gitignored — they're local working copies.
# To fetch the latest: npm run logs:list / npm run logs:fetch
# Or run the VS Code task: "GAS: Fetch Latest Log"
